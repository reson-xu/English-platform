# User Module

User account, authentication, and future account settings workflows belong here.

DTOs must stay under `internal/models/dto/user/`. Storage implementations must stay under `internal/adapters/storage/`.
