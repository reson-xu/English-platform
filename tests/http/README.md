# HTTP Tests

Backend behavior is verified by the user through HTTP/API requests at the end of a task.

Use this directory for optional request examples or HTTP test notes:

- Login and registration flows
- User role and permission checks
- Reading, writing, listening, and speaking module APIs
- AI workflow API contracts
- Learning record APIs

Codex should normally perform static compilation only unless the user explicitly asks for automated tests.
